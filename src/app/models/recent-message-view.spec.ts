import { RecentMessageView } from './recent-message-view';

describe('RecentMessageView', () => {
  it('should create an instance', () => {
    expect(new RecentMessageView()).toBeTruthy();
  });
});
