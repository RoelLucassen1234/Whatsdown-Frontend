import { RequestAnswerView } from './request-answer-view';

describe('RequestAnswerView', () => {
  it('should create an instance', () => {
    expect(new RequestAnswerView()).toBeTruthy();
  });
});
