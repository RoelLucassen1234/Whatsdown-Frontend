import { FriendRequestViewModel } from './friend-request-view-model';

describe('FriendRequestViewModel', () => {
  it('should create an instance', () => {
    expect(new FriendRequestViewModel()).toBeTruthy();
  });
});
