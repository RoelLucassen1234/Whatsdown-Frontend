import { PartlyPendingRequests } from './partly-pending-requests';

describe('PartlyPendingRequests', () => {
  it('should create an instance', () => {
    expect(new PartlyPendingRequests()).toBeTruthy();
  });
});
